---
---

<section id="contacto" class="py-20">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl sm:text-4xl font-bold text-light-blue dark:text-dark-blue mb-4">
        Descubre el Potencial de Nuestra Plataforma
      </h2>
      <p class="text-xl text-gray-700 dark:text-gray-300">
        Envíanos tus datos y un experto te mostrará cómo nuestra solución puede transformar tus procesos industriales.
      </p>
    </div>

    <form
      id="demo-form"
      class="bg-white dark:bg-dark-base p-8 rounded-lg shadow-lg"
      name="demo-form" method="POST" data-netlify-recaptcha="true" netlify-honeypot="bot-field" data-netlify="true"
    >
      <div>
        <p class="hidden">
          <label>
            Don’t fill this out if you’re human: <input name="bot-field" />
          </label>
        </p>
      </div>
      <div class="space-y-6">
        <div>
          <label
            for="name"
            class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
          >
            Nombre completo
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-light-green dark:focus:ring-dark-green focus:border-transparent dark:bg-gray-800"
          />
        </div>

        <div>
          <label
            for="email"
            class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
          >
            Correo electrónico
          </label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-light-green dark:focus:ring-dark-green focus:border-transparent dark:bg-gray-800"
          />
        </div>

        <div>
          <label
            for="telephone"
            class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
          >
            Teléfono
          </label>
          <input
            type="tel"
            id="telephone"
            name="telephone"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-light-green dark:focus:ring-dark-green focus:border-transparent dark:bg-gray-800"
          />
        </div>

        <div>
          <label
            for="company"
            class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
          >
            Empresa
          </label>
          <input
            type="text"
            id="company"
            name="company"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-light-green dark:focus:ring-dark-green focus:border-transparent dark:bg-gray-800"
          />
        </div>

        <div>
          <label
            for="message"
            class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
          >
            Mensaje
          </label>
          <textarea
            id="message"
            name="message"
            rows="4"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-700 rounded-lg focus:ring-2 focus:ring-light-green dark:focus:ring-dark-green focus:border-transparent dark:bg-gray-800"
          ></textarea>
        </div>

        <div data-netlify-recaptcha="true"></div>

        <button
          type="submit"
          class="w-full bg-light-green hover:bg-light-green/90 dark:bg-dark-green dark:hover:bg-dark-green/90 text-white px-6 py-3 rounded-lg text-lg font-semibold transition-colors"
        >
          Quiero una Demo
        </button>
      </div>
    </form>
  </div>
</section>

<!-- <script>
  const form = document.getElementById('demo-form');
  
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(e.target as HTMLFormElement);
    const data = Object.fromEntries(formData);
    
    try {
      // Aquí iría la lógica para enviar los datos al backend
      console.log('Datos del formulario:', data);
      alert('¡Gracias por unirte a nuestra lista de espera!');
      (e.target as HTMLFormElement).reset();
    } catch (error) {
      console.error('Error:', error);
      alert('Hubo un error al procesar tu solicitud. Por favor, intenta nuevamente.');
    }
  });
</script> -->
